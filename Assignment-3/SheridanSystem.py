class SheridanSystem():
    def __init__(self,noOfBuild):
        self.numOfBuildings = noOfBuild

    def run(noOfBuild):
        noOfBuild=int(input("Enter the number of Buildings : "))
        return noOfBuild